# Copy this file to config/.env and fill in real values
# Never commit config/.env — it is gitignored

QAENV=https://your-qa-environment.com
STAG=https://your-staging-environment.com
PROD=https://your-production-environment.com

# Credentials (use secrets manager in real enterprise)
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=changeme